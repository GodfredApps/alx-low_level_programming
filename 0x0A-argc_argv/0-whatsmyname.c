#include "main.h"
#include <stdio.h>

/**
 * main - entry point for function to print its name
 * @argc: argc parameter
 * @argv: an array of a command listed
 * Return: Always success
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s/n", argv[0]);

	return (0);
}

